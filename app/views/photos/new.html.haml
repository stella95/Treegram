%h1 Add a Photo
= form_for Photo.new(), :url => user_photos_path, :html => {:multipart => true} do |form|
  = form.file_field :image
  %br/
  = form.label 'Write a title'
  = form.text_field :title
  %br/
  = form.submit 'Upload'